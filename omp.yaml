# yaml-language-server: $schema=https://raw.githubusercontent.com/JanDeDobbeleer/oh-my-posh/main/themes/schema.json

version: 2
final_space: true
console_title_template: "{{ .Shell }} in {{ .Folder }}"
palette:
  red: "#cc241d"
  green: "#b8bb26"
  yellow: "#fabd2f"
  orange: "#d65d0e"
  blue: "#83a698"
  purple: "#d3869b"
  aqua: "#8ec07c"
  gray: "#a89984"
  fg: "#ebdbb2"
  white: "#fbf1c7"
blocks:
  - type: prompt
    alignment: left
    segments:
      - type: path
        style: plain
        template: "\n{{ .Path }} "
        newline: true
        properties:
          style: full
        foreground: p:fg
      - type: git
        style: plain
        templates:
          - "{{ .HEAD }} "
          - "{{ if .Staging.Changed }}{{ end }}{{ if .Working.Changed }}!{{ end }}{{ if gt .Working.Untracked 0 }}?{{ end }}{{ if gt .Behind 0 }}⇣{{ end }}{{ if gt .Ahead 0 }}⇡{{ end }}{{ if gt .StashCount 0 }}{{ end }} "
        properties:
          branch_icon: " "
          branch_max_length: 25
          commit_icon: "@"
          fetch_status: true
        foreground: p:purple
      - type: command
        template: "{{ .Output }} "
        style: plain
        properties:
          command: '[ -x "$HOME/.zsh/local-prompt.sh" ] && "$HOME/.zsh/local-prompt.sh"'
        foreground: p:blue
      - type: executiontime
        style: plain
        template: "{{ .FormattedMs }} "
        properties:
          threshold: 1000
        foreground: p:gray
    overflow: hidden
  - type: prompt
    alignment: left
    segments:
      - type: text
        style: plain
        foreground_templates:
          - "{{if eq .Code 0}}white{{end}}"
          - "{{if gt .Code 0}}p:red{{end}}"
        template: ❯
    newline: true
transient_prompt:
  foreground_templates:
    - "{{if gt .Code 0}}p:red{{end}}"
    - "{{if eq .Code 0}}p:fg{{end}}"
  template: "\n{{ now | date \"15:04:05\" }}❯ "
secondary_prompt:
  foreground_templates:
    - "{{if gt .Code 0}}p:red{{end}}"
    - "{{if eq .Code 0}}p:fg{{end}}"
  template: "❯❯ "
tooltips:
  - type: aws
    tips:
      - aws
      - terraform
    style: plain
    template: " {{ .Profile }}"
    properties:
      display_default: true
    foreground: p:aqua
  - type: kubectl
    tips:
      - k
      - kubectl
    style: plain
    foreground: p:aqua
    template: " {{.Context}}{{if .Namespace}}/{{.Namespace}}{{end}} "
  - type: gcp
    tips:
      - gcp
    style: plain
    foreground: p:aqua
    template: " {{.Project}} "
